{
  "name": "mizline",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:customer": "pnpm --filter @mizline/customer dev",
    "dev:kitchen": "pnpm --filter @mizline/kitchen dev",
    "dev:admin": "pnpm --filter @mizline/admin dev",
    "dev:api": "pnpm --filter @mizline/api dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
