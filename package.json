{
  "name": "mizline",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:kds": "pnpm --filter @mizline/kds dev",
    "dev:api": "pnpm --filter @mizline/api dev",
    "dev:customer": "pnpm --filter @mizline/customer dev",
    "dev:waiter": "pnpm --filter @mizline/waiter dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
