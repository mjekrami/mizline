NEXT_PUBLIC_API_URL="http://localhost:3003"

# Run: pnpm --filter @mizline/api exec prisma db seed
# Copy the printed Store and Table IDs below (not "1" — seed uses CUIDs).
NEXT_PUBLIC_DEMO_STORE_ID=""
NEXT_PUBLIC_DEMO_TABLE_ID=""
