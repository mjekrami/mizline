/** Wordmark aligned with mizline-landing branding (text-only, no icon mark). */
export const MIZLINE_WORDMARK = "mizline";

/** Default wordmark — matches landing `.logo` / KDS `KitchenLogo`. */
export const mizlineWordmarkClassName =
  "text-lg font-bold tracking-[-0.02em] text-foreground md:text-xl";

/** Centered splash wordmark — scaled up, same type treatment as KDS. */
export const mizlineWordmarkSplashClassName =
  "text-4xl font-bold tracking-[-0.02em] text-foreground sm:text-5xl";
