apiVersion: v1
kind: Service
metadata:
  name: site
  labels:
    app.kubernetes.io/name: site
    app.kubernetes.io/part-of: mizline
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: site
  ports:
    - name: http
      port: 3000
      targetPort: http
