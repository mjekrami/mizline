apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mizline
spec:
  rules:
    - host: api.mizline.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api
                port:
                  number: 3003
    - host: order.mizline.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: customer
                port:
                  number: 3000
    - host: kitchen.mizline.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kitchen
                port:
                  number: 3001
